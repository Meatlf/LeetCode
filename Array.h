#ifndef _ARRAY_H
#define _ARRAY_H
#include<vector>
#include<iostream>

namespace ARRAY {
	std::vector<int> sortArrayByParity(std::vector<int>& A);
	std::vector<int> sortedSquares(std::vector<int>& A);
}
#endif